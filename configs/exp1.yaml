name: "exp1"
ngpu: 1
workers: 2
runs: 2
epochs: 10

nn:
  num_layers: 3 # number of hidden layers
  # number of features for each linear layer, for decoder this will be reversed
  # len must be equal to num_layers
  features: [12, 8, 4]
  activation: relu
  loss: mse

dataset:
  filename: sjc_internacao.csv
  batch_size: 32

optimizer:
  lr: 0.001
  weight_decay: 0.0000001